---
cluster.name: nexus-elk
network.host: 0.0.0.0

# Nexus GCE cluster settings. Security is disabled because this repo does not
# include transport TLS material for cross-host Elasticsearch nodes.
xpack.security.enabled: false
node.store.allow_mmap: false

xpack.license.self_generated.type: trial
